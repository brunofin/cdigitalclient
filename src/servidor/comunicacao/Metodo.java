package servidor.comunicacao;

public enum Metodo {
	RESPOSTA,
	BAIXAR_FOTO, LISTAR_ITEMS, LISTAR_TIPOS, LISTAR_CATEGORIAS,		// metodos do servidor
	FAZER_PEDIDO;																//metodos do cliente
}
